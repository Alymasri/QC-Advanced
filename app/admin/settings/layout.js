export const metadata = {
	title: 'Settings | Admin Dashboard',
	description: 'QC Advanced Admin Dashboard Settings Page',
};

export default function AdminLayout({ children }) {
	return <div>{children}</div>;
}
