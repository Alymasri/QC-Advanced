export const metadata = {
	title: 'Reset Password | QC Authentication',
	description: 'QC Advanced Reset Password Page',
};

export default function AdminLayout({ children }) {
	return <div className="">{children}</div>;
}
